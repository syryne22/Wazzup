<form method="post" action="{{ path('app_salle_cinema_delete', {'idSalle': salle_cinema.idSalle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_cinema.idSalle) }}">
    <button class="btn">Delete</button>
</form>
